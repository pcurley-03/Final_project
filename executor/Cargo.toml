
[package]
name = "executor"
version = "0.1.0"
edition = "2021"

[dependencies]
fyrox = { workspace = true }
Final_project = { path = "../game", optional = true }

[features]
default = ["Final_project"]
dylib = ["fyrox/dylib"]
